CSS Variables
- Allows us to store variables to make it easier to maintain consistency and more easily make global style changes

How to define

--primary: #339af0;

How to use

background-clor: var(--primary);


CSS variables are set on the root pseudo-class

:root {
    --primary: #339af0;
}

CSS Calculations
- Perfomr dynamic calculations when setting values
