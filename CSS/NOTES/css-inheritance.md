CSS Inheritance
- Properties set on parent elements are passed to their childern by default

What gets inherited?
- It is mainly ttext properties that are inherited from parent to child
color, font-family, font-size, font-weight, font-style, line-heigt
text-align, text-transform

Inheritance Guidance
- Global font styles are set on the body element so that all child text elements inherit style by default
- Next, set gloabl fony styles on type selectors which will override inherited properties